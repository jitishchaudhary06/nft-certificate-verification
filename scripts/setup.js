#!/usr/bin/env node
/**
 * Helper script placeholders for ops tasks.
 * Usage examples documented in docs/DEPLOYMENT.md
 */
console.log("NFT Certificate Generator scripts ready.");
console.log("- prisma migrate / seed via npm run prisma:*");
console.log("- contract deploy via npm run deploy:contracts");
